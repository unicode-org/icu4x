// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

// Base enumerator definition
/** Additional information: [1](https://docs.rs/icu/latest/icu/calendar/struct.FromIxdtfError.html)
*/
export class FromIxdtfError {
    #value = undefined;

    static values = new Map([
        ["Unknown", 0],
        ["InvalidSyntax", 1],
        ["OutOfRange", 2],
        ["MissingFields", 3],
        ["UnknownCalendar", 4]
    ]);
    constructor(value) {
        if (value instanceof FromIxdtfError) {
            this.#value = value.value;
            return;
        }

        if (FromIxdtfError.values.has(value)) {
            this.#value = value;
            return;
        }

        throw TypeError(value + " is not a FromIxdtfError and does not correspond to any of its enumerator values.");
    }

    get value() {
        return this.#value;
    }

    get ffiValue() {
        return FromIxdtfError.values.get(this.#value);
    }

    static Unknown = new FromIxdtfError("Unknown");

    static InvalidSyntax = new FromIxdtfError("InvalidSyntax");

    static OutOfRange = new FromIxdtfError("OutOfRange");

    static MissingFields = new FromIxdtfError("MissingFields");

    static UnknownCalendar = new FromIxdtfError("UnknownCalendar");


    

}