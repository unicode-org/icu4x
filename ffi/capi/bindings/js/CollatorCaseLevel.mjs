// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

// Base enumerator definition
/** See the [Rust documentation for `CaseLevel`](https://docs.rs/icu/latest/icu/collator/enum.CaseLevel.html) for more information.
*/
export class CollatorCaseLevel {
    #value = undefined;

    static values = new Map([
        ["Auto", 0],
        ["Off", 1],
        ["On", 2]
    ]);

    constructor(value) {
        if (value instanceof CollatorCaseLevel) {
            this.#value = value.value;
            return;
        }

        if (CollatorCaseLevel.values.has(value)) {
            this.#value = value;
            return;
        }

        throw TypeError(value + " is not a CollatorCaseLevel and does not correspond to any of its enumerator values.");
    }

    get value() {
        return this.#value;
    }

    get ffiValue() {
        return CollatorCaseLevel.values.get(this.#value);
    }

    static Auto = new CollatorCaseLevel("Auto");
    static Off = new CollatorCaseLevel("Off");
    static On = new CollatorCaseLevel("On");
}