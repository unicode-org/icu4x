// generated by diplomat-tool
import { DataError } from "./DataError.mjs"
import { DataProvider } from "./DataProvider.mjs"
import { GraphemeClusterBreakIteratorLatin1 } from "./GraphemeClusterBreakIteratorLatin1.mjs"
import { GraphemeClusterBreakIteratorUtf16 } from "./GraphemeClusterBreakIteratorUtf16.mjs"
import { GraphemeClusterBreakIteratorUtf8 } from "./GraphemeClusterBreakIteratorUtf8.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";


/** An ICU4X grapheme-cluster-break segmenter, capable of finding grapheme cluster breakpoints
*in strings.
*
*See the [Rust documentation for `GraphemeClusterSegmenter`](https://docs.rs/icu/latest/icu/segmenter/struct.GraphemeClusterSegmenter.html) for more information.
*/

const GraphemeClusterSegmenter_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.ICU4XGraphemeClusterSegmenter_destroy(ptr);
});
export class GraphemeClusterSegmenter {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];
    
    
    constructor(ptr, selfEdge) {
        
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;
        // Unconditionally register to destroy when this object is ready to garbage collect.
        GraphemeClusterSegmenter_box_destroy_registry.register(this, this.#ptr);
    }

    get ffiValue() {
        return this.#ptr;
    }


    static create(provider) {
        
        const diplomat_receive_buffer = wasm.diplomat_alloc(5, 4);
        const result = wasm.ICU4XGraphemeClusterSegmenter_create(diplomat_receive_buffer, provider.ffiValue);
    
        try {
    
            if (!diplomatRuntime.resultFlag(wasm, diplomat_receive_buffer, 4)) {
                const cause = DataError[Array.from(DataError.values.keys())[diplomatRuntime.enumDiscriminant(wasm, diplomat_receive_buffer)]];
                throw new Error('DataError: ' + cause.value, { cause });
            }
            return new GraphemeClusterSegmenter(diplomatRuntime.ptrRead(wasm, diplomat_receive_buffer), []);
        } finally {
        
            wasm.diplomat_free(diplomat_receive_buffer, 5, 4);
        
        }
    }

    segmentUtf8(input) {
        
        const inputSlice = diplomatRuntime.DiplomatBuf.str8(wasm, input);
        
        // This lifetime edge depends on lifetimes 'a
        let aEdges = [this, inputSlice];
        const result = wasm.ICU4XGraphemeClusterSegmenter_segment_utf8(this.ffiValue, inputSlice.ptr, inputSlice.size);
    
        try {
    
            return new GraphemeClusterBreakIteratorUtf8(result, [], aEdges);
        } finally {
        
            inputSlice.garbageCollect();
        
        }
    }

    segmentUtf16(input) {
        
        const inputSlice = diplomatRuntime.DiplomatBuf.str16(wasm, input);
        
        // This lifetime edge depends on lifetimes 'a
        let aEdges = [this, inputSlice];
        const result = wasm.ICU4XGraphemeClusterSegmenter_segment_utf16(this.ffiValue, inputSlice.ptr, inputSlice.size);
    
        try {
    
            return new GraphemeClusterBreakIteratorUtf16(result, [], aEdges);
        } finally {
        
            inputSlice.garbageCollect();
        
        }
    }

    segmentLatin1(input) {
        
        const inputSlice = diplomatRuntime.DiplomatBuf.slice(wasm, input, "u8");
        
        // This lifetime edge depends on lifetimes 'a
        let aEdges = [this, inputSlice];
        const result = wasm.ICU4XGraphemeClusterSegmenter_segment_latin1(this.ffiValue, inputSlice.ptr, inputSlice.size);
    
        try {
    
            return new GraphemeClusterBreakIteratorLatin1(result, [], aEdges);
        } finally {
        
            inputSlice.garbageCollect();
        
        }
    }

    

}