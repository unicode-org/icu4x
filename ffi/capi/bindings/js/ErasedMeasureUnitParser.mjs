// generated by diplomat-tool
import { DataError } from "./DataError.mjs"
import { DataProvider } from "./DataProvider.mjs"
import { ErasedMeasureUnit } from "./ErasedMeasureUnit.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const ErasedMeasureUnitParser_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.icu4x_ErasedMeasureUnitParser_destroy_mv1(ptr);
});

/**
 * An ICU4X Erased Measure Unit Parser object, capable of parsing the CLDR unit identifier (e.g. `meter-per-square-second`) and get the {@link ErasedMeasureUnit}.
 *
 * See the [Rust documentation for `ErasedMeasureUnitParser`](https://docs.rs/icu/2.0.0/icu/experimental/measure/parser/struct.ErasedMeasureUnitParser.html) for more information.
 */
export class ErasedMeasureUnitParser {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("ErasedMeasureUnitParser is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            ErasedMeasureUnitParser_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Construct a new {@link ErasedMeasureUnitParser} instance using compiled data.
     *
     * See the [Rust documentation for `new`](https://docs.rs/icu/2.0.0/icu/experimental/measure/parser/struct.ErasedMeasureUnitParser.html#method.new) for more information.
     */
    #defaultConstructor() {

        const result = wasm.icu4x_ErasedMeasureUnitParser_create_mv1();

        try {
            return new ErasedMeasureUnitParser(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
        }
    }

    /**
     * Construct a new {@link ErasedMeasureUnitParser} instance using a particular data source.
     *
     * See the [Rust documentation for `new`](https://docs.rs/icu/2.0.0/icu/experimental/measure/parser/struct.ErasedMeasureUnitParser.html#method.new) for more information.
     */
    static createWithProvider(provider) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.icu4x_ErasedMeasureUnitParser_create_with_provider_mv1(diplomatReceive.buffer, provider.ffiValue);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new DataError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('DataError.' + cause.value, { cause });
            }
            return new ErasedMeasureUnitParser(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatReceive.free();
        }
    }

    /**
     * See the [Rust documentation for `parse`](https://docs.rs/icu/2.0.0/icu/experimental/measure/parser/struct.ErasedMeasureUnitParser.html#method.parse) for more information.
     */
    parse(unitId) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const unitIdSlice = diplomatRuntime.DiplomatBuf.str8(wasm, unitId);

        const result = wasm.icu4x_ErasedMeasureUnitParser_parse_mv1(this.ffiValue, ...unitIdSlice.splat());

        try {
            return result === 0 ? null : new ErasedMeasureUnit(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            functionCleanupArena.free();

        }
    }

    /**
     * Construct a new {@link ErasedMeasureUnitParser} instance using compiled data.
     *
     * See the [Rust documentation for `new`](https://docs.rs/icu/2.0.0/icu/experimental/measure/parser/struct.ErasedMeasureUnitParser.html#method.new) for more information.
     */
    constructor() {
        if (arguments[0] === diplomatRuntime.exposeConstructor) {
            return this.#internalConstructor(...Array.prototype.slice.call(arguments, 1));
        } else if (arguments[0] === diplomatRuntime.internalConstructor) {
            return this.#internalConstructor(...arguments);
        } else {
            return this.#defaultConstructor(...arguments);
        }
    }
}