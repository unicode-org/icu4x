// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

// Base enumerator definition
/** Additional information: [1](https://docs.rs/icu/latest/icu/calendar/struct.RangeError.html), [2](https://docs.rs/icu/latest/icu/calendar/enum.DateError.html)
*/
export class CalendarError {
    #value = undefined;

    static values = new Map([
        ["Unknown", 0],
        ["OutOfRange", 1],
        ["UnknownEra", 2],
        ["UnknownMonthCode", 3]
    ]);
    constructor(value) {
        if (value instanceof CalendarError) {
            this.#value = value.value;
            return;
        }

        if (CalendarError.values.has(value)) {
            this.#value = value;
            return;
        }

        throw TypeError(value + " is not a CalendarError and does not correspond to any of its enumerator values.");
    }

    get value() {
        return this.#value;
    }

    get ffiValue() {
        return CalendarError.values.get(this.#value);
    }

    static Unknown = new CalendarError("Unknown");

    static OutOfRange = new CalendarError("OutOfRange");

    static UnknownEra = new CalendarError("UnknownEra");

    static UnknownMonthCode = new CalendarError("UnknownMonthCode");


    

}