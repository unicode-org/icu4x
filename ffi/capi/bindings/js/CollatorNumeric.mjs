// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

// Base enumerator definition
/** See the [Rust documentation for `Numeric`](https://docs.rs/icu/latest/icu/collator/enum.Numeric.html) for more information.
*/
export class CollatorNumeric {
    #value = undefined;

    static values = new Map([
        ["Auto", 0],
        ["Off", 1],
        ["On", 2]
    ]);

    constructor(value) {
        if (value instanceof CollatorNumeric) {
            this.#value = value.value;
            return;
        }

        if (CollatorNumeric.values.has(value)) {
            this.#value = value;
            return;
        }

        throw TypeError(value + " is not a CollatorNumeric and does not correspond to any of its enumerator values.");
    }

    get value() {
        return this.#value;
    }

    get ffiValue() {
        return CollatorNumeric.values.get(this.#value);
    }

    static Auto = new CollatorNumeric("Auto");
    static Off = new CollatorNumeric("Off");
    static On = new CollatorNumeric("On");
}