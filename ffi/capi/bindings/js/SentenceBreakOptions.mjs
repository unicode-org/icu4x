// generated by diplomat-tool
import { Locale } from "./Locale.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";


/** See the [Rust documentation for `SentenceBreakOptions`](https://docs.rs/icu/latest/icu/segmenter/struct.SentenceBreakOptions.html) for more information.
*/
const SentenceBreakOptions_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.icu4x_SentenceBreakOptionsV1_destroy_mv1(ptr);
});

export class SentenceBreakOptions {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];
    
    constructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("SentenceBreakOptions is an Opaque type. You cannot call its constructor.");
            return;
        }
        
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;
        
        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            SentenceBreakOptions_box_destroy_registry.register(this, this.#ptr);
        }
    }

    get ffiValue() {
        return this.#ptr;
    }

    static create(locale) {
        const result = wasm.icu4x_SentenceBreakOptionsV1_create_mv1(locale.ffiValue);
    
        try {
            return new SentenceBreakOptions(diplomatRuntime.internalConstructor, result, []);
        }
        
        finally {}
    }
}