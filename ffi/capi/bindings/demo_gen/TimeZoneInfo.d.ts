import { TimeZoneInfo } from "icu4x"
export function timeZoneId(bcp47Id: string, offsetSeconds: number, dst: boolean);
export function zoneVariant(bcp47Id: string, offsetSeconds: number, dst: boolean);
