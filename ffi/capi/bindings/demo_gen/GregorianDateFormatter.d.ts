import { GregorianDateFormatter } from "icu4x"
import { IsoDate } from "icu4x"
import { Locale } from "icu4x"
export function formatIso(name: string, length: DateTimeLength, year: number, month: number, day: number);
