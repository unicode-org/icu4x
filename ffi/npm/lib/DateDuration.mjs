// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



/**
 * See the [Rust documentation for `DateDuration`](https://docs.rs/icu/2.1.1/icu/calendar/types/struct.DateDuration.html) for more information.
 */
export class DateDuration {
    #isNegative;
    get isNegative() {
        return this.#isNegative;
    }
    set isNegative(value){
        this.#isNegative = value;
    }
    #years;
    get years() {
        return this.#years;
    }
    set years(value){
        this.#years = value;
    }
    #months;
    get months() {
        return this.#months;
    }
    set months(value){
        this.#months = value;
    }
    #weeks;
    get weeks() {
        return this.#weeks;
    }
    set weeks(value){
        this.#weeks = value;
    }
    #days;
    get days() {
        return this.#days;
    }
    set days(value){
        this.#days = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new DateDuration(structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("DateDuration's constructor takes an object of DateDuration's fields.");
        }

        if ("isNegative" in structObj) {
            this.#isNegative = structObj.isNegative;
        } else {
            throw new Error("Missing required field isNegative.");
        }

        if ("years" in structObj) {
            this.#years = structObj.years;
        } else {
            throw new Error("Missing required field years.");
        }

        if ("months" in structObj) {
            this.#months = structObj.months;
        } else {
            throw new Error("Missing required field months.");
        }

        if ("weeks" in structObj) {
            this.#weeks = structObj.weeks;
        } else {
            throw new Error("Missing required field weeks.");
        }

        if ("days" in structObj) {
            this.#days = structObj.days;
        } else {
            throw new Error("Missing required field days.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 24;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 8;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof DateDuration) {
            return obj;
        }

        return DateDuration.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#isNegative, Uint8Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 4, this.#years, Uint32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 8, this.#months, Uint32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 12, this.#weeks, Uint32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 16, this.#days, BigUint64Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("DateDuration._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const isNegativeDeref = (new Uint8Array(wasm.memory.buffer, ptr, 1))[0] === 1;
        structObj.isNegative = isNegativeDeref;
        const yearsDeref = (new Uint32Array(wasm.memory.buffer, ptr + 4, 1))[0];
        structObj.years = yearsDeref;
        const monthsDeref = (new Uint32Array(wasm.memory.buffer, ptr + 8, 1))[0];
        structObj.months = monthsDeref;
        const weeksDeref = (new Uint32Array(wasm.memory.buffer, ptr + 12, 1))[0];
        structObj.weeks = weeksDeref;
        const daysDeref = (new BigUint64Array(wasm.memory.buffer, ptr + 16, 1))[0];
        structObj.days = daysDeref;

        return new DateDuration(structObj);
    }


    constructor(structObj) {
        return this.#internalConstructor(...arguments)
    }
}