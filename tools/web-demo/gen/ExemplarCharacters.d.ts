import { ExemplarCharacters } from "icu4x"
import { Locale } from "icu4x"
export function containsStr(selfLocaleName: string, s: string);
export function contains(selfLocaleName: string, cp: codepoint);
