import { Locale } from "icu4x"
import { Time } from "icu4x"
import { TimeFormatter } from "icu4x"
export function format(selfLocaleName: string, selfLength: DateTimeLength, selfTimePrecision: TimePrecision, selfAlignment: DateTimeAlignment, timeHour: number, timeMinute: number, timeSecond: number, timeSubsecond: number);
