import { Decimal } from "icu4x"
import { DecimalFormatter } from "icu4x"
import { Locale } from "icu4x"
export function format(selfLocaleName: string, selfGroupingStrategy: DecimalGroupingStrategy, valueV: string);
