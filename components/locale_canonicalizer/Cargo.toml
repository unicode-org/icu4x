# This file is part of ICU4X. For terms of use, please see the file
# called LICENSE at the top level of the ICU4X source tree
# (online at: https://github.com/unicode-org/icu4x/blob/master/LICENSE ).
[package]
name = "icu_locale_canonicalizer"
version = "0.1.0"
authors = ["Daniel Minor <dminor@mozilla.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
icu_locid = { version = "0.1", path = "../locid" }
icu_provider = { version = "0.1", path = "../provider" }

[dev-dependencies]
icu_locid_macros = { version = "0.1", path = "../locid/macros" }
icu_provider_cldr = { version = "0.1", path = "../provider_cldr" }
