# This file is part of ICU4X. For terms of use, please see the file
# called LICENSE at the top level of the ICU4X source tree
# (online at: https://github.com/unicode-org/icu4x/blob/main/LICENSE ).

[package]
name = "crate"
publish = false
version = "0.0.0"

[dependencies]
icu_capi = { version = "1.4", default-features = false, features = ["compiled_data", "icu_segmenter"] }
libc_alloc = { version = "1.0.6", features = ["global"] }
panic-halt = "0.2"
